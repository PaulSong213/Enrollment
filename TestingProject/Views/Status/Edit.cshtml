@model EnrollmentSystem.Models.StatusModel

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/Registrar_Layout.cshtml";
}

@if (ViewBag.SaveResult == true)
{
    <div class="alert alert-success">
        <strong>Success! </strong>Status Updated
    </div>
}
@{
ViewBag.SaveResult = false;
}
<div class="d-flex justify-content-between">
    <h3>@ViewBag.Title</h3>
    <a class="btn btn-primary" href="/Status">View Status</a>    
</div>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
        
        <div class="form-horizontal">
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            <div class="form-group my-3">
                @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" } })
                </div>
            </div>
            
            <div class="form-grou my-3">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Update Status" class="btn btn-default btn-success"  />
                </div>
            </div>
        </div>
}

